# 鳴潮 琴向け MIDI 自動演奏ツール 方針書

## 1. 目的

Standard MIDI File（SMF）を読み込み、MIDIノートの発音タイミングに合わせてWindowsのキーボード入力を自動生成し、鳴潮の琴を演奏する。

本ツールは汎用MIDIプレイヤーではなく、鳴潮の琴演奏に必要な機能だけを持つスタンドアロンアプリケーションとする。

---

## 2. 前提

### 2.1 鳴潮側のキー配置

鳴潮の琴は、Cメジャースケール相当の3オクターブ・21音を以下のキーで演奏する。

| 音域 | C | D | E | F | G | A | B |
|---|---|---|---|---|---|---|---|
| 上段 | Q | W | E | R | T | Y | U |
| 中段 | A | S | D | F | G | H | J |
| 下段 | Z | X | C | V | B | N | M |

### 2.2 発音仕様

- 鳴潮側はキー押下時に発音する。
- MIDIのノート長は演奏結果に影響しない。
- MIDI Note Onのみを発音イベントとして扱う。
- Note Offは無視する。
- Velocityが0のNote OnはNote Off相当として無視する。
- Velocity値による音量変化は扱わない。

### 2.3 MIDIデータの作成

- MIDIデータはDAW等で事前に作成する。
- 使用音は原則としてCメジャースケールの構成音に限定する。
- 演奏可能範囲はkeymapに定義された21音とする。
- 黒鍵や範囲外ノートは、再生前に検出できるようにする。

---

## 3. システム構成

```text
MIDIファイル（.mid）
        ↓
SMFパーサ
        ↓
Note Onイベント抽出
        ↓
tick → 絶対時刻変換
        ↓
MIDIノート → キーボードキー変換
        ↓
高精度スケジューラ
        ↓
Windows SendInput
        ↓
鳴潮
```

DAW、仮想MIDIポート、MIDIキーボードは再生時には不要とする。

---

## 4. 対応MIDI仕様

### 4.1 初期対応範囲

- SMF Type 0
- SMF Type 1
- PPQベースのタイミング
- Set Tempoメタイベント
- 複数トラックのイベント統合
- Note Onイベント
- Running Status

### 4.2 初期非対応

- SMPTEタイムコード形式
- MIDI Clock同期
- SysEx
- Program Change
- Control Change
- Pitch Bend
- Aftertouch
- 歌詞・マーカー等のメタイベント
- リアルタイムMIDI入力

非対応イベントは読み飛ばす。

---

## 5. イベント変換

### 5.1 MIDIイベント抽出

以下を満たすイベントのみ演奏対象とする。

```text
status = Note On
velocity > 0
note がkeymapに存在する
```

内部では、再生前に全イベントを絶対時刻へ変換する。

```cpp
struct PlayEvent {
    int64_t time_us;
    uint16_t scan_code;
};
```

### 5.2 テンポ処理

SMFのdelta tickを単純な固定時間へ変換せず、Set Tempoイベントを考慮して絶対時刻へ積算する。

Tempoイベントが存在しない場合は、SMF標準値である120 BPMを使用する。

### 5.3 同時発音

同一時刻のNote Onは和音として扱う。

同一時刻の全KeyDownを一つの`SendInput()`呼び出しにまとめ、その後に全KeyUpをまとめて送信する。

```text
時刻T:
  KeyDown A
  KeyDown D
  KeyDown G

T + pulse_ms:
  KeyUp A
  KeyUp D
  KeyUp G
```

---

## 6. キー入力方式

### 6.1 Windows入力API

Windowsの`SendInput()`を使用する。

仮想キーコードではなく、原則としてスキャンコード入力を使用する。

理由:

- ゲーム側の入力処理との互換性が比較的高い
- キーボード配列の影響を減らせる
- KeyDown / KeyUpを明示的に制御できる

### 6.2 キー押下時間

キー押下時間は設定ファイルで指定する。

初期値:

```text
pulse_ms = 20
```

MIDIノート長は使用せず、すべて固定長のキー入力へ変換する。

### 6.3 停止時処理

再生停止、エラー、アプリ終了時には、押下中として管理している全キーへKeyUpを送信する。

---

## 7. 設定ファイル

設定は`keymap.json`に保存する。

### 7.1 設定例

```json
{
  "pulse_ms": 20,
  "start_delay_ms": 3000,
  "unmapped_note": "error",
  "keymap": {
    "48": "Z",
    "50": "X",
    "52": "C",
    "53": "V",
    "55": "B",
    "57": "N",
    "59": "M",

    "60": "A",
    "62": "S",
    "64": "D",
    "65": "F",
    "67": "G",
    "69": "H",
    "71": "J",

    "72": "Q",
    "74": "W",
    "76": "E",
    "77": "R",
    "79": "T",
    "81": "Y",
    "83": "U"
  }
}
```

### 7.2 MIDIノート番号

初期設定では、中央段をC4～B4として扱う。

| MIDI Note | 音名 | キー |
|---:|---|---|
| 48 | C3 | Z |
| 50 | D3 | X |
| 52 | E3 | C |
| 53 | F3 | V |
| 55 | G3 | B |
| 57 | A3 | N |
| 59 | B3 | M |
| 60 | C4 | A |
| 62 | D4 | S |
| 64 | E4 | D |
| 65 | F4 | F |
| 67 | G4 | G |
| 69 | A4 | H |
| 71 | B4 | J |
| 72 | C5 | Q |
| 74 | D5 | W |
| 76 | E5 | E |
| 77 | F5 | R |
| 79 | G5 | T |
| 81 | A5 | Y |
| 83 | B5 | U |

DAWによってオクターブ表記が異なるため、設定・ログ・内部処理ではMIDIノート番号を基準とする。

---

## 8. 未割当ノートの処理

`unmapped_note`で処理方法を選択する。

### 8.1 error

未割当ノートが存在する場合、再生を開始しない。

再生前に以下を表示する。

```text
Unmapped notes:
  MIDI 38: 2 events
  MIDI 40: 2 events
  MIDI 66: 7 events
```

制作したMIDIを正確に検証する用途では、これをデフォルトとする。

### 8.2 ignore

未割当ノートを無視して再生する。

### 8.3 nearest

最も近い割当済みノートへ量子化する。

黒鍵を白鍵へ寄せる用途を想定するが、旋律や和声が変化するため補助機能扱いとする。

### 8.4 octave_fold

範囲外ノートを1オクターブ単位で演奏範囲内へ移動する。

ただし、折り返した結果が黒鍵または未割当ノートの場合は別途処理が必要となる。

初期版では`error`と`ignore`のみ実装し、`nearest`と`octave_fold`は後回しとする。

---

## 9. 再生制御

### 9.1 必須操作

- MIDIファイルを開く
- 再生
- 停止
- 再生前検証
- 終了

### 9.2 再生開始遅延

鳴潮のウィンドウへフォーカスを移す時間を確保するため、再生開始前にカウントダウンを設ける。

初期値:

```text
start_delay_ms = 3000
```

### 9.3 ホットキー

初期版では以下を候補とする。

| 操作 | キー |
|---|---|
| 再生開始 | F6 |
| 停止 | F7 |
| 緊急停止 | Esc |

鳴潮側で使用するQ～U、A～J、Z～Mとは重複させない。

---

## 10. タイミング実装

### 10.1 再生基準時計

`QueryPerformanceCounter()`を使用する。

再生開始時刻を基準に、各イベントの`time_us`まで待機する。

### 10.2 待機方式

長い待機ではSleep系APIを使用し、イベント直前のみ短時間のspin waitを行う。

例:

```text
残り時間 > 2 ms:
    Sleep

残り時間 <= 2 ms:
    spin wait
```

閾値は実測により調整する。

### 10.3 遅延時の処理

OSスケジューリング等により予定時刻を過ぎた場合、イベントを破棄せず即時送信する。

連続して大きな遅延が発生した場合は、ログへ記録する。

---

## 11. UI方針

初期版は最小限のGUIまたはCLIとする。

### 11.1 最小GUI案

```text
MIDI File: [ fugue.mid        ] [Open]

Status:
  Duration: 42.0 sec
  Note On events: 322
  Max polyphony: 4
  Unmapped notes: 0

[Validate] [Play] [Stop]

Start delay: 3000 ms
Pulse width: 20 ms
```

### 11.2 CLI案

```shell
wuthering-midi-player.exe fugue.mid --keymap keymap.json
```

CLIの場合も、開始遅延と緊急停止手段を用意する。

初期実装はCLIでも成立するが、ゲーム利用時の操作性を考えると最終的には小規模GUIが望ましい。

---

## 12. ログ

以下を出力する。

- MIDIファイル名
- SMF Type
- PPQ
- 演奏時間
- Note Onイベント数
- 最大同時発音数
- テンポイベント数
- 未割当ノート一覧
- 再生開始・終了時刻
- 最大スケジューリング遅延
- SendInput失敗

デバッグ時にはイベント単位のログを有効化できるようにする。

---

## 13. 安全策

- 鳴潮以外のウィンドウへ誤入力しないよう、再生前に対象ウィンドウを確認する。
- オプションとして、鳴潮がフォアグラウンドでない場合は再生を停止する。
- 停止時には必ず全キーを解放する。
- 緊急停止キーはグローバルホットキーとして登録する。
- MIDIファイル読込時にイベント数と再生時間へ上限を設ける。

---

## 14. 実装言語・ライブラリ案

### 14.1 本実装

C++を推奨する。

理由:

- `SendInput()`と`QueryPerformanceCounter()`を直接扱える
- タイミング制御が容易
- 単一EXEとして配布しやすい
- 外部ランタイム依存を減らせる

### 14.2 SMFパーサ

候補:

- Craig Sapp / Midifile
- 自前最小SMFパーサ

初期版では既存の軽量ライブラリを利用し、鳴潮固有部分へ注力する。

### 14.3 JSON

候補:

- nlohmann/json

### 14.4 GUI

候補:

- Win32 API
- Dear ImGui
- Qt

規模を考えると、初期版はCLIまたはWin32の小規模GUIで十分。

---

## 15. 開発フェーズ

### Phase 1: キー入力検証

- `SendInput()`で鳴潮の琴が発音することを確認
- KeyDown / KeyUp間隔を評価
- 同時押しが正常に和音として認識されることを確認
- スキャンコード入力の可否を確認

### Phase 2: 固定イベント再生

- ハードコードした時刻列から演奏
- `QueryPerformanceCounter()`によるスケジューリング
- 再生、停止、全キー解放

### Phase 3: SMF読込

- Type 0対応
- Note On抽出
- PPQ・Tempo変換
- 同時刻イベントのグループ化

### Phase 4: keymap.json対応

- MIDIノート番号とキーの外部設定
- pulse幅、開始遅延の設定
- 未割当ノート検出

### Phase 5: Type 1・UI対応

- 複数トラック統合
- 再生前検証画面
- ファイル選択
- ホットキー
- ログ表示

### Phase 6: 補助機能

- transpose
- octave shift
- nearest quantize
- octave fold
- 再生速度変更
- 曲中位置指定

---

## 16. MVP仕様

最初の完成条件は以下とする。

- Windows上で動作する
- `.mid`ファイルを指定できる
- SMF Type 0を読み込める
- Set Tempoを考慮できる
- Note Onのみ抽出する
- `keymap.json`に従ってキー入力へ変換する
- 同時発音を処理できる
- 開始3秒後に再生する
- F7またはEscで停止できる
- 停止時に全キーを解放する
- 未割当ノートがあれば再生前にエラー表示する

---

## 17. 今回のMIDIデータへの適用

対象ファイル:

```text
fugue.mid
```

本ファイルを再生する前に、以下を確認する。

- 全Note Onがkeymap内に収まっているか
- 黒鍵が残っていないか
- 下限・上限を超えるノートがないか
- 最大同時発音数が鳴潮側で受理されるか
- 最短発音間隔に対して`pulse_ms`が長すぎないか

範囲外ノートが意図的なものである場合は、DAW側でオクターブ移動または削除を行う。

---

## 18. 方針まとめ

本ツールは、SMFを直接読み込んでNote Onイベントだけを鳴潮のキー入力へ変換する。

必要な主要要素は次の4点だけである。

1. SMFの時刻解析
2. Note Onイベント抽出
3. `keymap.json`によるノート・キー変換
4. `SendInput()`による高精度なキー入力

MIDI再生、音源、DAW連携、仮想MIDIポート、Note Off処理は不要であり、鳴潮専用に割り切ることで小規模かつ堅牢な実装が可能である。
